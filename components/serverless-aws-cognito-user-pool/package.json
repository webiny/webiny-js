{
  "name": "@webiny/serverless-aws-cognito",
  "version": "1.0.0",
  "main": "serverless.js",
  "license": "MIT",
  "dependencies": {
    "@webiny/serverless-component-tracking": "^1.0.0",
    "execa": "^2.0.3",
    "lodash.isequal": "^4.5.0"
  },
  "peerDependencies": {
    "aws-sdk": "^2.539.0"
  },
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp serverless.js package.json dist/",
    "prepublishOnly": "yarn build"
  }
}
