{
  "name": "@webiny/serverless-aws-cognito-user-pool",
  "version": "0.1.0",
  "main": "serverless.js",
  "license": "MIT",
  "dependencies": {
    "@webiny/tracking": "^0.1.0",
    "execa": "^2.0.3",
    "lodash.isequal": "^4.5.0"
  },
  "peerDependencies": {
    "aws-sdk": "^2.539.0"
  },
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp serverless.js package.json dist/",
    "prepublishOnly": "yarn build"
  },
  "publishConfig": {
    "access": "public"
  }
}
