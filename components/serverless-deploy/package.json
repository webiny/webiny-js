{
  "name": "@webiny/serverless-deploy",
  "version": "0.0.0",
  "main": "serverless.js",
  "license": "MIT",
  "dependencies": {
    "@serverless/core": "^1.1.1",
    "@webiny/serverless-template": "^0",
    "chalk": "^2.4.2"
  },
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp serverless.js dist/",
    "postbuild": "cp package.json README.md LICENSE dist/",
    "prepublishOnly": "yarn build"
  },
  "publishConfig": {
    "access": "public"
  }
}
