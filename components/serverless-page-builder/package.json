{
  "name": "@webiny/serverless-page-builder",
  "version": "0.1.12",
  "main": "serverless.js",
  "license": "MIT",
  "dependencies": {
    "@serverless/core": "^1.1.1",
    "@webiny/api-page-builder": "^0.1.6",
    "@webiny/serverless-apollo-service": "^0.3.0",
    "@webiny/tracking": "^0.1.5",
    "load-json-file": "^6.2.0"
  },
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp serverless.js package.json dist/",
    "prepublishOnly": "yarn build"
  },
  "publishConfig": {
    "access": "public"
  },
  "adio": {
    "ignore": {
      "dependencies": [
        "@webiny/api-page-builder"
      ]
    }
  }
}
