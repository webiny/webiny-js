const path = require("path");
const { Component } = require("@serverless/core");

class ApolloService extends Component {
    async default(inputs = {}) {
        // TODO: generate apollo service boilerplate code
    }
}

module.exports = ApolloService;
