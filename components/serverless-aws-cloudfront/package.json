{
  "name": "@webiny/serverless-aws-cloudfront",
  "version": "0.1.2",
  "main": "serverless.js",
  "license": "MIT",
  "dependencies": {
    "@serverless/aws-cloudfront": "^6.0.0",
    "@serverless/core": "^1.1.1"
  },
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp serverless.js dist/",
    "postbuild": "cp package.json README.md LICENSE dist/",
    "prepublishOnly": "yarn build"
  },
  "publishConfig": {
    "access": "public"
  }
}
