{
  "name": "@webiny/serverless-api-gateway",
  "version": "1.0.0",
  "main": "serverless.js",
  "license": "MIT",
  "dependencies": {
    "@serverless/core": "^1.1.1",
    "@serverless/aws-api-gateway": "^2.0.0"
  },
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp serverless.js package.json dist/",
    "prepublishOnly": "yarn build"
  }
}
