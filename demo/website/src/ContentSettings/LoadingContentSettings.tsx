import React from "react";
import styled from "@emotion/styled";

const Loading = styled.div`
    // Add CSS styles here
`;

export const LoadingContentSettings = () => {
    return <Loading>Loading content settings...</Loading>;
};
