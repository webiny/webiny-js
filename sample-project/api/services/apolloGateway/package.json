{
  "name": "service-apollo-gateway",
  "version": "0.1.0",
  "scripts": {
    "build": "webiny run build"
  },
  "dependencies": {
    "@webiny/handler": "^0.1.0",
    "@webiny/handler-apollo-gateway": "^0.1.0"
  }
}
