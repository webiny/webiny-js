# Context

Read `AGENTS.md`.

# Skills

When implementing API features (paths: `packages/api**` or `packages/*/src/api/**`), use the `/backend-developer` skill (`.claude/skills/backend-developer/SKILL.md`).

When implementing Admin features (paths: `packages/app**` or `packages/*/src/admin/**`), use the `/admin-developer` skill (`.claude/skills/admin-developer/SKILL.md`).
