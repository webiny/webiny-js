---
name: tester
description: Use when running tests. Shows how to run tests for a single package.
user_invocable: true
---

# Running Tests

To test a package, use:

```bash
yarn test packages/<package-name>
```

Example:

```bash
yarn test packages/api-core
```
