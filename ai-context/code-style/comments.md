# Comments

Use `//` for single-line comments and `/* ... */` for multi-line comments. Always end comments with a period.

```ts
// Good: single-line, ends with a period.

/*
 * Good: multi-line block.
 */
```

```ts
// bad: no trailing period
# bad: wrong comment syntax
```
