{
  "name": "webiny-project-client",
  "scripts": {
    "develop": "./../../node_modules/.bin/webiny-scripts develop-app src --require=babel-register",
    "production:build": "./../../node_modules/.bin/webiny-scripts build-app src --require=babel-register",
    "production:serve": "./../../node_modules/.bin/webiny-scripts serve src --require=babel-register",
    "deploy": "scotty --source=dist/production --nocdn --update=true --region=eu-west-1",
    "clean": "rimraf node_modules/react node_modules/react-dom",
    "postinstall": "yarn clean"
  },
  "devDependencies": {
    "rimraf": "^2.6.2",
    "webpack-visualizer-plugin": "^0.1.11"
  },
  "dependencies": {
    "jquery": "^3.3.1"
  }
}
