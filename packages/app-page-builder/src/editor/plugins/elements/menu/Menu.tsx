console.log("HELLO WORLD!!!");
import * as React from "react";

const Menu = ({data}) => {
    console.log("HELLO WORLD!!!");
    
    return (
        <h1>HELLO WORLD</h1>
    );
};

export default React.memo(Menu);