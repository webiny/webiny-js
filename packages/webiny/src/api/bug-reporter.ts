export { IssueDrafter } from "@webiny/bug-reporter/api/drafter/abstractions.js";

/* Plain wire types with no abstraction behind them, so they have no namespace to live in. */
export type { IBugReportPayload } from "@webiny/bug-reporter/shared/types.js";
export type { IReportedScreenshot } from "@webiny/bug-reporter/shared/types.js";
