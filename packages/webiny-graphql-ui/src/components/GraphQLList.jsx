import React from "react";
import { createComponent } from "webiny-app";

class GraphQLList extends React.Component {
    constructor() {
        super();
        this.state = {};
    }

    render() {
        return (

        );
    }
}

export default createComponent(GraphQLList, {modules: []});