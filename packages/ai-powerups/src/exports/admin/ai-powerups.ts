// GetSettings (admin) — read AI Power Ups settings on the client, e.g. to list the
// configured Writer Personas for a picker.
export { GetSettingsFeature } from "~/admin/features/settings/getSettings/feature.js";
