{
  "name": "@webiny/serverless-component",
  "version": "0.1.0",
  "license": "MIT",
  "dependencies": {},
  "scripts": {
    "build": "rimraf dist && mkdir dist && cp -r package.json Component.js index.js LICENSE README.md dist/",
    "prepublishOnly": "yarn build"
  },
  "publishConfig": {
    "access": "public",
    "directory": "."
  }
}
