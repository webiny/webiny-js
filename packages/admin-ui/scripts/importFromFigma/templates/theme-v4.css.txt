@import url("https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap");
@import "tailwindcss";

@theme {
  /* Color. */
  {COLOR}

  /* Background color. */
  {BG_COLOR}

  /* Border color. */
  {THEME_BORDER_COLOR}

  /* Border radius. */
  {THEME_BORDER_RADIUS}

  /* Border width. */
  {THEME_BORDER_WIDTH}

  /* Fill. */
  {FILL}

  /* Font. */
  {FONT}

  /* Font weight. */
  {FONT_WEIGHT}

  /* Margin. */
  {THEME_MARGIN}

  /* Ring color */
  {RING_COLOR}

  /* Ring width */
  {RING_WIDTH}

  /* Padding. */
  {THEME_PADDING}

  /* Spacing. */
  {THEME_SPACING}

  /* Text color. */
  {TEXT_COLOR}

  /* Text sizes. */
  {TEXT_SIZE}
}

/*focus-visible:ring-lg focus-visible:ring-primary-dimmed*/

body {
    /* Styles all text that was not rendered via the Text component. */
    @apply font-sans antialiased text-md text-neutral-primary;
}

a {
  /* Styles all links that were not rendered via the Link component. */
  @apply text-accent-primary hover:underline focus-visible:outline-none rounded-xs;
}

#root {
    @apply isolate;
}

b,
strong {
    @apply font-semibold;
}

i,
em {
    @apply italic;
}