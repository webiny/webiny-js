@import url("https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap");
@import "tailwindcss";

@theme {
  /* Color. */
  {COLOR}

  /* Background color. */
  {BG_COLOR}

  /* Border color. */
  {THEME_BORDER_COLOR}

  /* Border radius. */
  {THEME_BORDER_RADIUS}

  /* Border width. */
  {THEME_BORDER_WIDTH}

  /* Fill. */
  {FILL}

  /* Font. */
  {FONT}

  /* Font weight. */
  {FONT_WEIGHT}

  /* Margin. */
  {THEME_MARGIN}
  
  /* Padding. */
  {THEME_PADDING}

  /* Spacing. */
  {THEME_SPACING}

  /* Text color. */
  {TEXT_COLOR}

  /* Text sizes. */
  {TEXT_SIZE}
}
