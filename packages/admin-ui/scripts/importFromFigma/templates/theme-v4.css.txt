@import url("https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap");
@import "tailwindcss";

@theme {
  /* Border radius. */
  {THEME_BORDER_RADIUS}

  /* Font. */
  {THEME_FONT}

  /* Spacing. */
  {THEME_SPACING}

  /* Colors. */
  {THEME_COLORS}

  /* Text sizes. */
  {THEME_TEXT_SIZES}
}

@import url("https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap");

:root {
  /* Colors. */
  {COLORS}

  /* Background colors. */
  {BACKGROUND_COLOR}

  /* Border width. */
  {BORDER_WIDTH}

  /* Padding. */
  {PADDING}

  /* Text color. */
  {TEXT_COLOR}

  /* Text size. */
  {TEXT_SIZE}
}

