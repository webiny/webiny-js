import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';

import * as CardStories from './Card.stories';

<Meta of={CardStories} />

# Card

A Card is used to display content or actions in a focused, overlay window that requires user interaction before returning to the main interface. Modals are valuable for drawing attention to important information, capturing user input, and streamlining workflows without navigating away from the current page.

## Usage

<Canvas of={CardStories.Documentation} source={ { code: `
import React, { useState } from "react";
import { Card, Button } from "@webiny/admin-ui";

const CardExample = () => {
    const [open, setOpen] = useState(false);

    return (
        <>
            <Button
                variant="primary"
                text="Open Card"
                onClick={() => setOpen(true)}
            />

            <Card
                open={open}
                onOpenChange={open => setOpen(open)}
                title="Card Title"
                description="A short card description."
                info={<>Learn more about this <a href={"#"}>here</a>.</>}
                actions={
                    <>
                        <Card.CancelAction />
                        <Card.ConfirmAction />
                    </>
                }
                showCloseButton={true}
                dismissible={true}
            >
                This is the card content area. You can place any content here including
                forms, text, images, or other components.
            </Card>
        </>
    );
};

export default CardExample;

` } }
        additionalActions={[
            {
                title: 'Open in GitHub',
                onClick: () => {
                    window.open(
                        'https://github.com/webiny/webiny-js/blob/feat/new-admin-ui/packages/admin-ui/src/Card/Card.tsx',
                        '_blank'
                    );
                },
            }
        ]}
/>

<Controls of={CardStories.Documentation} exclude={["onOpenChange", "trigger"]}/>

```tsx
import React, { useState } from "react";
import { Card, Button } from "@webiny/admin-ui";

const CardExample = () => {
    const [open, setOpen] = useState(false);

    return (
        <>
            <Button
                variant="primary"
                text="Open Card"
                onClick={() => setOpen(true)}
            />

            <Card
                open={open}
                onOpenChange={open => setOpen(open)}
                title="Card Title"
                description="A short card description."
                info={<>Learn more about this <a href={"#"}>here</a>.</>}
                actions={
                    <>
                        <Card.CancelAction />
                        <Card.ConfirmAction />
                    </>
                }
                showCloseButton={true}
                dismissible={true}
            >
                This is the card content area. You can place any content here including
                forms, text, images, or other components.
            </Card>
        </>
    );
};

export default CardExample;
```

## Examples

### Controlled Visibility

Controlled Visibility demonstrates how to manually control the open/close state of the Card component from the outside, using React useState. Here we are following the “controlled component” pattern, where the visibility (open state) of the Card is not managed internally by the component itself, but instead controlled by the parent using React state.

<Canvas of={CardStories.ControlledVisibility} source={ { code: `
import React, { useState } from "react";
import { Card, Button } from "@webiny/admin-ui";

const ControlledVisibilityExample = () => {
    const [open, setOpen] = useState(false);

    return (
        <>
            <Button
                variant="primary"
                text="Open Card"
                onClick={() => setOpen(true)}
            />

            <Card
                open={open}
                onOpenChange={open => {
                    if (!open) {
                        setOpen(false);
                    }
                }}
                title="Card Title"
                description="A short card description."
                actions={
                    <>
                        <Card.CancelAction />
                        <Card.ConfirmAction />
                    </>
                }
            >
                This card&apos;s visibility is controlled by the open state.
            </Card>
        </>
    );
};

export default ControlledVisibilityExample;
` } } />

### With Dropdown Menu

<Canvas of={CardStories.WithDropdownMenu} source={ { code: `
import React, { useState } from "react";
import { Card, Button, DropdownMenu } from "@webiny/admin-ui";

const WithDropdownMenuExample = () => {
    const [open, setOpen] = useState(false);

    return (
        <>
            <DropdownMenu trigger={<Button variant="primary" text="Open Menu" />}>
                <DropdownMenu.Item text={"Open Card"} content="Open Card" onClick={() => setOpen(true)} />
            </DropdownMenu>

            <Card
                open={open}
                onOpenChange={() => setOpen(false)}
                title="Card Title"
                description="A short card description."
                actions={
                    <>
                        <Card.CancelAction />
                        <Card.ConfirmAction />
                    </>
                }
            >
                This card is opened from a dropdown menu item.
            </Card>
        </>
    );
};

export default WithDropdownMenuExample;
` } } />

### Without Close Button

<Canvas of={CardStories.WithoutCloseButton} source={ { code: `
import React, { useState } from "react";
import { Card, Button } from "@webiny/admin-ui";

const WithoutCloseButtonExample = () => {
    const [open, setOpen] = useState(false);

    return (
        <>
            <Button
                variant="primary"
                text="Open Card"
                onClick={() => setOpen(true)}
            />

            <Card
                open={open}
                onOpenChange={open => setOpen(open)}
                title="Card Title"
                description="A short card description."
                showCloseButton={false}
                actions={
                    <>
                        <Card.CancelAction />
                        <Card.ConfirmAction />
                    </>
                }
            >
                This card doesn&apos;st have a close button in the top-right corner.
            </Card>
        </>
    );
};

export default WithoutCloseButtonExample;
` } } />

### Alert Card

<Canvas of={CardStories.AlertCard} source={ { code: `
import React, { useState } from "react";
import { Card, Button } from "@webiny/admin-ui";

const AlertCardExample = () => {
    const [open, setOpen] = useState(false);

    return (
        <>
            <Button
                variant="primary"
                text="Open Alert Card"
                onClick={() => setOpen(true)}
            />

            <Card
                open={open}
                onOpenChange={open => setOpen(open)}
                title="Confirm Action"
                description="Are you sure you want to delete this item?"
                showCloseButton={false}
                actions={
                    <>
                        <Card.CancelAction text="Cancel" />
                        <Card.ConfirmAction text="Delete" />
                    </>
                }
            >
                <p>This action cannot be undone.</p>
                <p>Deleted items cannot be recovered.</p>
            </Card>
        </>
    );
};

export default AlertCardExample;
` } } />

### With Icon

<Canvas of={CardStories.WithIcon} source={ { code: `
import React, { useState } from "react";
import { Card, Button } from "@webiny/admin-ui";
import { ReactComponent as DoorbellIcon } from "@webiny/icons/ring_volume.svg";

const WithIconExample = () => {
    const [open, setOpen] = useState(false);

    return (
        <>
            <Button
                variant="primary"
                text="Open Card with Icon"
                onClick={() => setOpen(true)}
            />

            <Card
                open={open}
                onOpenChange={open => setOpen(open)}
                title="Card with Icon"
                description="This card has an icon in the header."
                icon={<Card.Icon icon={<DoorbellIcon />} label="Title icon" />}
                actions={
                    <>
                        <Card.CancelAction />
                        <Card.ConfirmAction />
                    </>
                }
            >
                The icon helps to visually identify the purpose of this card.
            </Card>
        </>
    );
};

export default WithIconExample;
` } } />

### Prevent Outside Dismiss

<Canvas of={CardStories.PreventOutsideDismiss} source={ { code: `
import React, { useState } from "react";
import { Card, Button } from "@webiny/admin-ui";

const PreventOutsideDismissExample = () => {
    const [open, setOpen] = useState(false);

    return (
        <>
            <Button
                variant="primary"
                text="Open Modal Card"
                onClick={() => setOpen(true)}
            />

            <Card
                open={open}
                onOpenChange={open => setOpen(open)}
                title="Important Action"
                description="This card requires explicit action."
                dismissible={false}
                actions={
                    <>
                        <Card.CancelAction />
                        <Card.ConfirmAction />
                    </>
                }
            >
                This card cannot be dismissed by clicking outside or pressing Escape.
                You must use one of the action buttons to close it.
            </Card>
        </>
    );
};

export default PreventOutsideDismissExample;
` } } />

### With Tabs

<Canvas of={CardStories.WithTabs} source={ { code: `
import React, { useState } from "react";
import { Card, Button, Tabs } from "@webiny/admin-ui";

const WithTabsExample = () => {
    const [open, setOpen] = useState(false);

    return (
        <>
            <Button
                variant="primary"
                text="Open Card with Tabs"
                onClick={() => setOpen(true)}
            />

            <Card
                open={open}
                onOpenChange={open => setOpen(open)}
                title="Settings"
                description="Configure your preferences"
                bodyPadding={false}
                actions={
                    <>
                        <Card.CancelAction />
                        <Card.ConfirmAction text="Save" />
                    </>
                }
            >
                <Tabs
                    spacing="lg"
                    tabs={[
                        <Tabs.Tab
                            key="account"
                            value="account"
                            trigger="Account"
                            content="Make changes to your account here."
                        />,
                        <Tabs.Tab
                            key="company"
                            value="company"
                            trigger="Company"
                            content="Make changes to your company info here."
                        />,
                        <Tabs.Tab
                            key="security"
                            value="security"
                            trigger="Security"
                            content="Make changes to your security settings here."
                        />,
                        <Tabs.Tab
                            key="development"
                            value="development"
                            trigger="Development"
                            content="Make changes to your development settings here."
                        />
                    ]}
                />
            </Card>
        </>
    );
};

export default WithTabsExample;
` } } />

## Anatomy

### Card Construction
<img src="/images/storybook/card/anatomy.png" alt="Anatomy"/>

### Overlay and Positioning
<img src="/images/storybook/card/overlay-and-positioning.png" alt="Card Overlay and Positioning"/>

## Usage
<img src="/images/storybook/card/usage.png" alt="Usage"/>
