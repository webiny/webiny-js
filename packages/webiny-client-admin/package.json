{
  "name": "webiny-client-admin",
  "version": "0.0.0-semantically-released",
  "main": "lib/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/webiny/webiny-js.git"
  },
  "files": [
    "lib/"
  ],
  "author": "Pavel Denisjuk",
  "license": "MIT",
  "dependencies": {
    "invariant": "^2.2.4",
    "isomorphic-fetch": "^2.2.1",
    "js-cookie": "^2.2.0",
    "randomstring": "^1.1.5",
    "webiny-client": "0.0.0-semantically-released",
    "webiny-client-ui": "0.0.0-semantically-released"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0"
  },
  "scripts": {
    "build": "babel src -d lib --source-maps --copy-files",
    "build:watch": "babel src -d lib --source-maps --copy-files --watch"
  }
}
