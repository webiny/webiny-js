## Schema Generation

This folder contains few tests to verify that there are no errors in the GraphQL base (without models) and populated (
with a model) response.

Test was done due to some error in the values object which was caused by wrongly assigned generated resolves.
