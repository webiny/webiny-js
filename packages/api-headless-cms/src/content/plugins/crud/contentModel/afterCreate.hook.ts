import { CmsContentModelType, CmsContext } from "@webiny/api-headless-cms/types";

// eslint-disable-next-line
export const afterCreateHook = async (context: CmsContext, model: CmsContentModelType) => {};
