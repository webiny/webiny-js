{
  "name": "@webiny/tracking",
  "version": "0.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "load-json-file": "^6.2.0",
    "request": "^2.88.0",
    "uuid": "^3.3.3",
    "write-json-file": "^4.2.0"
  },
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp index.js package.json dist/",
    "prepublishOnly": "yarn build"
  }
}
