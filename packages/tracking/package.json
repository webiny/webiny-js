{
  "name": "@webiny/tracking",
  "version": "0.1.1",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "load-json-file": "^6.2.0",
    "request": "^2.88.0",
    "uuid": "^3.3.3",
    "write-json-file": "^4.2.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp index.js package.json LICENSE README.md dist/",
    "prepublishOnly": "yarn build"
  }
}
