import React from "react";
import { IconButton, IconButtonProps } from "@webiny/admin-ui/Button";

export const ButtonFloating = (props: IconButtonProps) => {
    return <IconButton {...props} variant={"primary"} />;
};
