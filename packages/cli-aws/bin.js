// Dummy file so that yarn does not complain about missing "bin.js" file.
// The real bin is built from src/bin.ts to dist/bin.js, and the package is published from `dist`
// (so the `bin` field's "./bin.js" resolves to the built file).
