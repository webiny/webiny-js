import { token } from "./token";
import { themeAccordion as Collapse } from "../Accordion";
import { themeButton as Button } from "../Button";

export const theme = {
    token,
    components: {
        Button,
        Collapse
    }
};
