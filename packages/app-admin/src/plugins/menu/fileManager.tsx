/**
 * Leaving just a dummy plugin, in case someone still imports from this path.
 */
export default () => {
    console.log("[DEPRECATED] @webiny/app-admin/plugins/menu/fileManager is no longer used.");
    return { type: "dummy" };
};
