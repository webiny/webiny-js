/**
 * @deprecated Use `useTheme` from "./useTheme.js". Kept as a thin re-export for backward
 * compatibility.
 */
export {
    useTheme,
    useTheme as useThemeMode,
    THEME_KEY,
    THEME_KEY as THEME_MODE_KEY
} from "./useTheme.js";
export type { UseTheme } from "./useTheme.js";
