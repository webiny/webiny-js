{
  "name": "demo-api-gateway",
  "version": "1.0.0",
  "dependencies": {
    "@apollo/gateway": "^0.6.12"
  },
  "scripts": {
    "build": "cross-env NODE_ENV=production npx webpack --out build"
  }
}
